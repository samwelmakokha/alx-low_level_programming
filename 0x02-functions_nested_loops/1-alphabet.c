#include "main.h"
/**
 * print_alphabet - main entry point of the function
 *
 * Return: returns void
 */
void print_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
